Application: Append Smoke
  Description: v0.9.2 L3 smoke test — verifies the Append CRUD verb compiles end-to-end. Field-targeted access rule produces an APPEND route at POST /<resource>/{id}/<field>:append. No ai-agent compute here — that's L7's smoke. Just exercise the grammar and the route generation.
Id: 7e1b3a2c-4f9d-4e1a-b3c5-1d2e8f4a9c01

Identity:
  Scopes are "chat.use"
  An "anonymous" has "chat.use"

Content called "chat_threads":
  Each chat_thread has a title which is text, default "Conversation"
  Each chat_thread has a conversation which is conversation
  Anyone with "chat.use" can view chat_threads
  Anyone with "chat.use" can create chat_threads
  Anyone with "chat.use" can append to chat_threads.conversation

As anonymous, I want to see threads so that I can verify append works:
  Show a page called "Threads"
    Display a table of chat_threads with columns: title
